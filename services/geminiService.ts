// This service has been disabled as per the requirement to use frontend-only logic.
export const analyzeTextWithGemini = async (text: string): Promise<string> => {
  return "AI Analysis is disabled.";
};